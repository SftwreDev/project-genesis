# Server
GENESIS_PORT=8787

# Optional SQLite location (default: ~/.genesis/genesis.db)
# GENESIS_DB_PATH=/path/to/genesis.db
# GENESIS_DATA_DIR=/path/to/data-dir

# Dev-only: Vite dev server port when using `make dev`
GENESIS_FRONTEND_PORT=3310
